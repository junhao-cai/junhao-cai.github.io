---
import PublicationsView from '../components/shell/PublicationsView.astro';
import { getLangFromUrl } from '../i18n';

const lang = getLangFromUrl(Astro.url);
---

<PublicationsView lang={lang} />
